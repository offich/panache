import 'package:flutter/material.dart';

class PanacheColor {
  static const Color primaryColor = Color.fromRGBO(244, 232, 105, 1.0);
  static const Color secondaryColor = Color.fromRGBO(250, 242, 211, 1.0);
  static const Color thirdlyColor = Color.fromRGBO(92, 210, 230, 1.0);

  static Color errorColor = Colors.red[300]!;
}
